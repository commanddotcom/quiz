version: '3.8'

services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./data:/app/data
      - ./tests.json:/app/tests.json
      - ./index.html:/app/index.html
      - ./app.js:/app/app.js
    restart: unless-stopped
    container_name: test-app
