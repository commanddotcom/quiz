# Конфігурація для вашого тест субдомену
# Цей файл треба скопіювати в /var/www/moblik/docker/nginx/conf.d/

server {
    listen 80;
    server_name your-test-subdomain.com;  # ЗАМІНІТЬ на ваш субдомен
    
    location / {
        proxy_pass http://host.docker.internal:8081;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Після отримання SSL сертифіката розкоментуйте:
# server {
#     listen 443 ssl;
#     server_name your-test-subdomain.com;
#     
#     ssl_certificate /etc/nginx/ssl/your-test-subdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/your-test-subdomain.com/privkey.pem;
#     
#     location / {
#         proxy_pass http://host.docker.internal:8081;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
